
  # KTSA

  This is a code bundle for KTSA. The original project is available at https://www.figma.com/design/DLOrRJTBALfKTbXcRogrVb/KTSA.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  